# OOP Assignment 2 - Custom Classes in C++
## Overview

This project implements three custom classes from scratch in C++:
- **VectorType** — A 3D vector class with full operator overloading
- **String** — A custom string class with dynamic memory management
- **RomanNumerals** — A Roman numeral class with arithmetic and conversion

All classes are built without relying on standard library containers
